# 固件源码来源与交付范围

日期：2026-09-14。目标：BORING MIST，`wired-macro-pad-v1` / `WMP-S3-MATRIX12-POWER-V2`，构建目标 `matrix12-power-v2-codex-usb`，产品版本 `0.3.0-alpha.1`。

参考固件为 `20260913.01-gd9bab6f0-dirty`，验证等级 `sample-verified`。这是有明确样机验收范围的固件，不是所有设备与修改的通用稳定性保证。

此次导出当前工作区的完整应用运行源码、构建配置、锁定的组件依赖声明、构建与自定义 ZIP 打包工具，以及其需要的公开协议常量头；同时包含 2026-09-14 的默认 `custom-` 构建和无 Git 源码 ZIP 支持。它是当前源码快照，含参考固件之后的工具变更；不声称重建得到官方镜像或与参考镜像逐字节一致。

运行时 `.c/.h`、配置和构建工具按选定文件列表复制，不为公开版关闭设备认证、更新校验或电源保护。`protocol/generated/protocol_contract.h` 来自同一工作区，供固件编译使用；本次不升级现有社区控制台源码、SDK 或协议接口版本。

公开范围包括编译所需的内嵌显示资源；不另附内部设计源文件、字体素材包、产品模型和制造资料。实际许可与品牌使用边界见 [LICENSING.md](../LICENSING.md)。

不包含生产签发/预置工具、设备私钥、发布私钥、服务器凭据、真实设备证书和台账、用户配置备份、历史二进制或内部维修教程。第三方组件由 ESP-IDF 组件管理器按声明获取，保留各自许可。

源码阅读、构建通过、维护包导入和实体设备功能验收是不同结果。此次公开交付不刷写实体设备、不改变服务器发布、不更新设备身份，也不把 DIY 固件标记成官方发布。构建与包检查结果见 [本次验证记录](../docs/firmware-source-verification.md)。
