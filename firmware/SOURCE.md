# 固件源码来源与交付范围

日期：2026-09-20。目标：BORING MIST，`wired-macro-pad-v1` / `WMP-S3-MATRIX12-POWER-V2`，构建目标 `matrix12-power-v2-codex-usb`，产品版本 `0.3.0-alpha.1`。

参考固件为 `20260916.01-gbc6d2699-dirty`，验证等级 `sample-verified`，来源基准提交为 `bc6d269980d471449f651ad9b36511abf62aeb45`。该官方构建明确保留 `git_dirty=true`；此次公开的是与该包**功能对应**的源码快照，包含 80–160 MHz 动态调频、保存亮度生效、默认 15 分钟待机、蓝牙广播降频与待机轮询调整。由于原包不是干净提交构建，公开源码不能保证重建出逐字节相同的官方镜像；后续拍摄及装配候选改动没有混入此快照。这是有明确样机验收范围的固件，不是所有设备与修改的通用稳定性保证。

公开范围为既有应用运行源码、构建配置、锁定的组件依赖声明、构建与自定义 ZIP 打包工具，以及其需要的公开协议常量头。preview.9 仅同步上述五项产品固件改动；NORMAL 模式 Agent 键行为沿用 preview.7。自定义构建继续使用 `custom-` 编号，不能冒充官方镜像。

运行时 `.c/.h`、配置和构建工具按选定文件列表复制，不为公开版关闭设备认证、更新校验或电源保护。`protocol/generated/protocol_contract.h` 来自同一工作区，供固件编译使用；本次不升级现有社区控制台源码、SDK 或协议接口版本。

公开范围包括编译所需的内嵌显示资源；不另附内部设计源文件、字体素材包、产品模型和制造资料。实际许可与品牌使用边界见 [LICENSING.md](../LICENSING.md)。

不包含生产签发/预置工具、设备私钥、发布私钥、服务器凭据、真实设备证书和台账、用户配置备份、历史二进制或内部维修教程。第三方组件由 ESP-IDF 组件管理器按声明获取，保留各自许可。

源码阅读、构建通过、维护包导入和实体设备功能验收是不同结果。此处源码交付不刷写实体设备、不更新设备身份，也不把 DIY 固件标记成官方发布。另附的 [官方签名维护包](../docs/official-firmware-20260916.01.md) 与可修改源码分开交付；构建与包检查结果见 [本次验证记录](../docs/firmware-source-verification.md)。
