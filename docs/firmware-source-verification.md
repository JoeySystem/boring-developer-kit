# 固件源码公开验证

## preview.9 · 20260916.01 源码与官方维护包

日期：2026-09-20。本轮从产品源码中仅选择与 `20260916.01-gbc6d2699-dirty` 权威包相关的五项改动：Power V2 动态调频、保存亮度、未保存待机默认值、蓝牙广播间隔和待机轮询。后续拍摄/装配候选改动未同步。该包于 2026-09-16 在留样机完成普通 USB OTA、回连及 Build ID 读回，用户确认快速输入没有可感延迟；功耗收益、Windows 和长期运行未量化或扩展验收。

公开目录使用 ESP-IDF 6.0.2 对 `matrix12-power-v2-codex-usb` 实际编译成功，生成 `custom-public1601-…` 自定义应用镜像及 ZIP；没有刷写设备。固件工具测试 **9 项通过**，公开材料测试 **12 项通过**。完整开发包生成 **545 个条目**；路径检查没有发现预编译镜像、构建产物、生产签发/预置工具、凭据或设备备份。SDK 1.0.0 wheel 重新构建成功，三个扩展 ZIP 由同一源码打包。

官方 `Boring-20260916.01.zip` 经现有控制台发布公钥验签通过；包内仅有签名 manifest、应用镜像和中文使用说明。镜像字节数、SHA-256、硬件标识、Build ID 与 manifest 一致。它是 Release 独立附件；源码开发包不内含官方镜像。因为官方构建含 `git_dirty=true`，公开源码与其**功能对应**，不称逐字节可复现。

## preview.7 · 20260914.03 源码同步

日期：2026-09-14。本轮把公开应用固件从 `20260913.01` 功能参考对齐到当前 `20260914.03-gbc6d2699-dirty / sample-verified` 权威参考，开放范围不扩大。

- 同步 4 个已有公开运行文件：`action_engine.c`、`codex_micro_ble.c`、`codex_micro.h` 和 `codex_micro_controls.h`。它们与当前私有工作区对应文件逐字节一致，没有复制新的目录或工具。
- 新行为仅涉及 NORMAL 模式六个 Agent 键的输入路由、标准状态颜色及断线／未知回白色；NORMAL 其他控件继续走 Profile，CODEX / CC 既有行为不变。
- 当前权威包既有验收记录为固件与共享协议 792 passed、421 subtests passed，Power V2 ESP-IDF 6.0.2 构建成功，样机普通 USB CDC A/B OTA、运行 Build ID、硬件 ID、目标槽位和无回滚状态一致，并由用户确认本轮实体行为。该结果属于官方权威包，不等于公开仓库生成的 DIY 镜像已刷机。
- 来源工程中针对同步代码的主机回归 **95 passed**；公开固件工具 **9 项通过**，公开材料 **12 项通过**。
- 在公开仓库目录使用 ESP-IDF 6.0.2 实际编译 `matrix12-power-v2-codex-usb --custom-name public1403` 成功，生成 `custom-public1403-20260914.01-g14006922-dirty`，应用镜像 1,140,512 字节，维护 ZIP 仅含自定义应用镜像和 manifest。本地测试产物不进入 Git 或 Release。

本轮没有发布预编译固件、刷写设备、修改身份／eFuse、更新服务器渠道或加入生产签发工具。preview.6 已完成的无 Git 源码 ZIP 编译入口没有改动；本轮未重复该项实际编译。DIY 安装与恢复官方的实体闭环仍未验收，相关限制继续以 [DIY 修改边界](../firmware/DIY-GUIDE.md) 为准。

## preview.6 · 首次公开固件源码

日期：2026-09-14。此轮新增固件源码与 DIY 边界说明，保留已有社区控制台 0.1.24、SDK 1.0.0 和旧安装下载，不将它们改称 0.1.29。

## 实际完成

- 从当前固件工程选择复制 179 个运行、构建配置和工具测试文件，运行源码不作公开版降级；另同步编译所需的协议常量头。没有复制历史固件、设备数据或生产签发/预置工具。
- 在独立公开仓库目录使用 ESP-IDF **6.0.2** 实际构建 `matrix12-power-v2-codex-usb --custom-name publiccheck` 成功。依赖由组件管理器获取，未使用私有固件目录中的构建产物。
- 生成 `custom-publiccheck-20260914.01-g9c95d08e-dirty`，产品版本 `0.3.0-alpha.1`，应用镜像 **1,140,368 字节**。其中 Git 编号来自构建时的公开仓库工作树，不冒充官方构建。
- 自定义 ZIP 检查通过：只含应用镜像与 manifest，`origin=custom`、`validation_state=built`、最低控制台版本 0.1.24，镜像中包含同一 custom 构建编号。此测试镜像不作为官方固件分发。
- `python3 -m unittest discover -s firmware/tools/tests -p 'test_*.py'`：**9 项通过**，涵盖默认/命名/官方构建选择、无 Git 源码 ZIP 入口及现有镜像 manifest 检查。构建入口测试使用编译器替身；上面的实际 ESP-IDF 构建是另一项独立证据。
- `python -m pytest -q tests/test_public_materials.py`：**12 项通过**。包括公开认证资料、示例、源码包内容、许可与更新配置边界；新增固件源码必须入包，编译目录和固件二进制不得混入源码包。

- 完整开发包 ZIP 独立解压后，在没有 `.git` 的目录用 ESP-IDF 6.0.2 再次实际编译成功，生成 `custom-zipcheck-20260914.01-nogit`；包标记 `git_commit=unavailable`、`git_dirty=true`，ZIP 仍只包含应用与 manifest。没有依赖私人仓库或手工补文件。
- SDK 1.0.0 wheel 沿用同一源码重新构建成功，供本次 Release 的既有 SDK 下载入口使用；SDK 功能未修改。

## 发布检查

发布前检查源码开发 ZIP 内容、选定运行源码与来源文件逐项一致、相对文档链接以及凭据/本机路径排除。公开说明不提供硬件恢复操作教程，禁止改动基础模块，不承诺任意修改都能恢复。代码与发布说明经过限定范围的只读审阅。

## 尚未验收

此次没有刷写实体设备、执行固件恢复、改动设备身份或发布服务器渠道。完整 DIY 安装 → 恢复官方 → 实际功能正常、配置格式兼容性、Windows 原生安装与恢复，以及任意损坏固件的控制台恢复均不属于此次已通过范围。

官方控制台 0.1.29 的既有记录止于实际服务器查询、下载、验签及恢复确认后取消，不能替代本次公开源码构建的实体安装验收。本仓库旧 0.1.13 安装包不应被当作新恢复流程的交付包。

发布者没有在此轮修改固件逻辑、构建保护或认证检查，也没有改动既有公开控制台功能；源码开放与官方产品固件发布分别管理。
