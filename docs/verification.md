> 最新源码开发包：**preview.10 / Console 0.2.1 / SDK 1.1.0**，见 [本轮验证](verification-console-0.2.1.md)。以下保留历次记录。

# 发布验证记录

## v0.1.0-preview.9 · 固件 20260916.01

2026-09-20，公开源码仅同步 Power V2 `20260916.01` 对应的五项功能改动；来源权威包的样机验收范围及源码不可逐字节复现的原因见[来源说明](../firmware/SOURCE.md)。官方签名维护 ZIP 作为 Release 独立附件，源码开发 ZIP 不含官方镜像；新源码构建仅生成 `custom-` 包。此次不重新刷写实体设备，不把后续候选或公开构建标记为官方固件。

本轮发布的具体源码构建、测试和附件检查记录见[固件源码公开验证](firmware-source-verification.md)。Console 源码与安装版保持既有版本，相关验证仍见下方 preview.8 历史记录。

## v0.1.0-preview.8 · Console 0.1.46 公开源码同步

2026-09-15，将控制台公开适配源码同步到 Console 0.1.46 的已验收来源状态；固件应用源码继续对齐 `20260914.03-gbc6d2699-dirty / sample-verified`，本轮核对后没有新的固件源码差异。公开程序继续使用 Community 独立标识、系统字体和简化设备示意，在线应用／固件源及内置官方固件发布记录均为空。

环境：Apple Silicon Mac、Python 3.12、PySide6 6.11.1。最终一次完整 Console 无窗口回归为 **1416 passed、9 skipped / 881.13 秒**。9 项跳过分别依赖 Cocoa 原生关闭重开、原生激活和菜单坐标，或需要官方 Sparkle 框架；本轮没有把它们写作通过。根目录公开材料与固件工具合计 **22 passed、1 个既有 pytest 警告**。

本轮审阅发现并修正两个公开发布边界问题：测试生成的 `console/output/` 截图原本可能被递归打入开发包，现已由测试临时目录、忽略规则、打包排除和 ZIP 断言共同移除；状态灯键原本可通过配置文件导入绕过界面限制，现由草稿 API、候选验证和 Profile 绑定统一拒绝，不能进入设备写入。相关配置导入、扩展提案、外部配置刷新、写入事务和布局回归均包含在上述全量结果中。

Console 0.1.46 与 SDK 1.0.0 wheel 构建成功。完整开发包生成 544 个条目，不含测试输出、固件构建产物、预编译固件、DMG / EXE、密钥、设备备份、生产签发／预置／部署工具或保留品牌素材。将开发包解压到独立临时目录后，清除原仓库源码路径，仅从包内加载 Console、SDK 和协议；模拟设备进入 READY，主窗口启动与渲染通过。

来源工程的 0.1.46 macOS 安装版、USB 认证和两次配置写入／读回已有独立验收；该记录用于说明同步来源，不等于本次 Community 源码已形成并验收新的原生安装包。本轮没有连接或刷写实体设备，没有发布 0.1.46 DMG / EXE 或官方固件镜像，也没有修改官方在线更新渠道。公开安装包仍为 0.1.13；Windows、Linux、Intel Mac、真实 USB / BLE、自定义固件安装与恢复闭环仍不在本次验收范围。

## v0.1.0-preview.7 · Power V2 20260914.03 源码

2026-09-14，将公开应用固件源码对齐到当前 `20260914.03-gbc6d2699-dirty / sample-verified` 权威参考。同步范围是已有公开目录中的 4 个运行文件，未新增生产、签发、预置、部署或维修工具；详细来源、构建和验证边界见 [固件源码公开验证](firmware-source-verification.md)。

本轮公开目录实际 ESP-IDF 6.0.2 编译通过；来源工程相关行为回归 95 项、公开固件工具 9 项和公开材料 12 项通过。生成的是 `custom-` 测试构建，不是官方镜像，也未刷入设备。当前权威包已有普通 USB OTA、运行身份／槽位／无回滚及本轮实体功能验收，这些证据只说明参考权威包，不自动覆盖用户修改后的 DIY 固件。

DIY 边界、非商业许可和 Best Effort 维护方式不变。未发布新的 DMG / EXE 或预编译固件，未修改官方在线更新渠道；完整 DIY 安装 → 恢复官方 → 功能正常的实机闭环仍未验收。

## v0.1.0-preview.6 · 固件源码

实际 ESP-IDF 构建、自定义 ZIP 和公开包检查结果见 [固件源码验证](firmware-source-verification.md)。下方 preview.5 及更早记录为历史范围，其中“不包含固件源码”仅适用于当时的开发包。

## v0.1.0-preview.5 · Console 0.1.24 源码

2026-09-13，从已归档的 `333b6fb103d864243d602a45056138184e235521` 同步可公开代码。保留公开认证资料、系统字体、独立应用标识和空更新配置；不附内部模型、品牌动画、完整固件或生产数据。来源与差异见 [控制台源码](console-source.md)。

环境：Apple Silicon Mac、Python 3.12、PySide6 6.11.1。使用公开目录，模拟设备与临时测试签名，不连接硬件。

### 源码检查

- 执行 `QT_QPA_PLATFORM=offscreen:configfile=console/tests/offscreen.json console/.venv/bin/python -m pytest -q console/tests tests`，覆盖 1335 个用例。收尾全量结果为 1318 passed、10 failed、7 skipped；另有 2 个 teardown error，属于同一组缺少认证字段的模拟对象用例。
- 随后修正剩余问题并定向复测 **60 passed / 31.47 秒**：包含上述全部失败用例及相关翻译、编辑、窗口缩放、重连用例。修正包括语言过滤器销毁事件处理、短屏示意图缩放、更新后的模式文案/滚动行为断言，以及模拟设备认证字段。最终修正后未再次重跑整套全量；不能把分批结果称为一次全量全绿。
- 此前已完成三语言布局及提示词引导 **77 passed**，模拟 HTTPS 更新、发布者签名、确认写入和翻译相关 **72 passed**。这些是分批记录，数量不相加作为独立用例总数。
- 7 项跳过需要 Cocoa 原生窗口/菜单或实际 Sparkle 框架，offscreen 不替代其验收。原生平台和实体设备流程仍保留以下限制。

测试已去除内部固件打包/发布工具依赖，改用公开包结构与临时签名数据验证客户端；没有将这些内部工具一并导出。

### 独立交付检查

- Console 0.1.24 与 SDK 1.0.0 wheel 构建成功；应用 wheel 的 111 个源码/资源文件与公开目录逐项比对。wheel 仍需根目录协议和 SDK，不是独立 DMG / EXE。
- 独立解压开发包、新建虚拟环境，清除 PYTHONPATH 后按文档 editable 安装。应用、SDK 和协议均从解压目录加载，模拟设备界面启动与渲染通过；资源、公开边界、文案、示意图和缩放等 **44 项检查通过**。
- 提交文件检查覆盖私钥、个人路径、生产数据与保留素材；空应用/固件源和公开包排除项有回归检查。文档相对链接可解析。

### 未验收范围

本轮没有发布或验收新 DMG / EXE，没有刷写设备、更新官方服务或修改固件权威指针。公开官方安装包仍是 0.1.13。

未覆盖：0.1.24 安装版原生关闭/重开和弹出菜单、macOS/Windows 实际应用替换、实体 USB/BLE 重连和固件升级、配置保留、三个扩展示例实体触发、干净电脑、Windows/Linux/Intel Mac/Python 3.13，以及任意自定义固件的控制台恢复。源码和模拟流程通过不等于这些项目通过。

---

## v0.1.0-preview.4 / console-v0.1.13

来源为私人产品仓库已提交的 Console 0.1.13 `8a243971a10a3bd6cdf985f2e800e61e072dc0b7`。使用三方合并同步变更，保留公开适配；SDK / API 不变，固件 `.07` 指针未变。仅新增可选电量协议说明，不包含候选固件或内部交付记录。

本轮环境：Apple Silicon Mac、Python 3.12、PySide6 6.11.1。旧轮次结果不计作本轮重跑。

- 自动测试：`QT_QPA_PLATFORM=offscreen:configfile=console/tests/offscreen.json console/.venv/bin/python -m pytest -q console/tests tests`：**1027 passed, 6 skipped**（382.78 秒）。6 项跳过均依赖原生 Cocoa 窗口生命周期／激活／弹出菜单坐标，本轮无窗口测试不计其为通过。
- 首次全量检查发现 6 项旧断言仍依赖固定按键尺寸、旧导航、旧滚动布局或断线后清空工作区；按新版行为更新，并为旧固件蓝牙失败增加首次连接／已有离线草稿两种用例，继续检查提示、禁写和停止重连。布局用例等待实际异步布局完成。
- Console 0.1.13 与 SDK 1.0.0 wheel 构建成功。独立解压开发包、新建虚拟环境，按文档 editable 安装 SDK 与控制台、清除 PYTHONPATH 后启动模拟设备成功；不依赖私人仓库。wheel 不是独立应用，仍需协议资源。
- 原始 0.1.13 DMG 为 79,049,136 字节，arm64，严格 codesign 检查通过，认证策略为 production。只读挂载中的 Runner 实际执行并导入 SDK API 1.0 / Qt 6.11.1，完成后已卸载镜像。
- 安装交付 ZIP 中的 DMG 与原始文件逐字节相同，22 份第三方许可文本完整保留。运行依赖版本与上轮相同，许可原文复用归档目录；安装 ZIP 随附完整文本。文档相对链接均可解析。

官方 0.1.13 安装版已有 USB 认证与配置读取、最大化／恢复、按键／旋钮／摇杆选择、隐藏重开及控件比例验收；本轮核对同一 DMG 的版本、arm64、production 认证策略与签名完整性。没有改写设备配置或刷固件。

仍未完成：新版本完整扩展实体触发、NORMAL 快捷键物理执行、完整升级回归、真实电量候选固件、Windows / Intel Mac / 干净电脑及 Apple 公证。Windows 提示词自动粘贴仍未实现；源码存在平台分支不代表 Windows 交付通过。

---

## 官方安装交付包 console-v0.1.11

2026-09-11 新增官方 0.1.11 Apple Silicon Mac 测试版下载。它与下方源码开发包独立交付。复用该版本已有的实际安装、USB 认证与配置读取、窗口缩放和中英文布局验收记录；上传前对同一 DMG 重新进行只读包检查，版本 0.1.11、arm64、codesign 严格验证、production 认证策略及独立 Runner 均确认存在。包内 Runner 已在清除 PYTHONPATH 的环境实际执行，成功导入 SDK API 1.0 和 Qt 6.11.1；这是包内运行依赖检查，不是扩展实体触发验收。原始 DMG 为 78,837,455 字节，未改写程序。

本轮只整理发布文件和说明，不重跑未改动源码的完整测试，不将公开源码 0.1.6 的 983 项通过数用于 0.1.11 安装版。详细安装方法、已知英文文案问题和未验收项目见 [安装说明](install-console.md)。

---

## v0.1.0-preview.3

本轮使用 Console 0.1.6 已提交快照 `1b0b16b28ddf69fafa8a2621331a69bc49df30c9` 进行公开适配，SDK 1.0.0 / API 1.0 保持不变，固件参考仍是 `.07 / sample-verified`。

本轮环境：Apple Silicon Mac、Python 3.12、PySide6 6.11.1。以下为本轮新增证据，不沿用历史测试数量。

| 验证 | 结果与范围 |
|---|---|
| 公开目录独立安装与构建 | 新建虚拟环境安装本地 SDK 和控制台成功；控制台 0.1.6 与 SDK 1.0.0 wheel 构建成功 |
| 完整开发包解压后运行 | 在独立临时目录新建另一虚拟环境，清除 PYTHONPATH 后安装成功；应用、SDK 和协议均从该解压目录加载；模拟设备界面启动和渲染通过，资源、SDK 与公开包结构定向测试 21 项通过 |
| 社区版与官方版并存 | 分开应用设置、单实例、本地扩展 API、登录启动项和 Claude Hook 记录；Hook 回归 16 项通过，包含两版安装／卸载互不删除对方记录 |
| USB 内部留样机只读连接 | 公开社区源码完成 HELLO、能力读取、生产证书验证、随机挑战、状态与配置读取；设备运行 `.07`。本轮未刷写固件、写设备身份或改设备配置 |
| 开发包内容 | 完整 ZIP 包含控制台源码、SDK、协议、示例与说明；不包含虚拟环境、内部字库、官方图标、完整固件或设备备份 |

社区控制台与公开材料完整回归：**983 passed / 6 skipped，252.82 秒**。跳过项需要 Cocoa 原生窗口外观、重新打开、激活与弹出菜单坐标，offscreen 不能代替其验收。执行命令：

```bash
QT_QPA_PLATFORM=offscreen:configfile=console/tests/offscreen.json console/.venv/bin/python -m pytest -q console/tests tests
```

本轮复核发现并修复了灯光／设置工作区在尺寸调整过程中重排导致的旧几何残留；改为当前 resize/show 结束后按最终尺寸排列。另修正测试 LanguageManager 生命周期，避免共享 QApplication 累积翻译器和事件过滤器。修复后完整布局与灯光两文件 62 项、原复现顺序 61 项均通过，再完成上述完整回归。未放宽窗口尺寸或布局断言。

**尚未完成：**三个扩展示例在最终安装包中的真实按键触发、绑定回退及提案写入／读回联合验收；社区版原生 DMG / EXE 构建与安装验收；BLE 扩展实体触发；干净电脑、Windows / Linux / Intel Mac / Python 3.13 实测。USB 认证和配置读取通过不代表这些项目通过。本次只发布源码开发者预览，不发布新的官方安装包或固件镜像。

开发者可按 [控制台 README](../console/README.md) 准备环境和运行相关测试；源码构建无需访问私人项目。源码宿主、安装包、合成事件和真实设备触发的结果应分别记录。

---

## v0.1.0-preview.2 历史发布验证

2026-09-11 对齐控制台0.1.6源码与 Power V2 `.07 sample-verified` 固件。本次 SDK 1.0.0 / API 1.0 未变化，保留公开示例与已去除测试私钥的测试向量。

- 当前控制台源码的 SDK、扩展平台、Runner、管理、manifest、本地 API、事件路由、绑定、上下文和提案定向回归：89项通过。
- 本公开仓库独立测试：11项通过，覆盖 Schema、认证公开正反例、公钥与测试根分离、交付 ZIP/许可及提案示例驻留。
- SDK 两个运行文件与当前内部源码逐字节一致。协议正文同步可选只读 `stack_diagnostics`；SDK不因此新增原始设备控制能力。
- 使用现有工具重新生成完整开发包、三个扩展ZIP与SDK wheel；不包含产品安装包或固件镜像。
- 内部0.1.6安装版已完成普通USB/蓝牙连接、旧.16经USB升级到.07、自动回连、配置保留及窗口/菜单验收。该结果仅用于配套版本说明，不能替代三个扩展示例的实体触发验收。
- 干净电脑、Windows/Linux、Python3.13、安装版扩展实体触发与BLE扩展触发仍未覆盖。许可和支持边界保持不变。

以下保留上一预发布的完整验证范围，避免将旧结果当作本轮重跑。

## v0.1.0-preview.1 历史发布验证

本记录对应本次 SDK / 协议预发布。验证环境为 macOS 26.6.2 / Apple Silicon、Python 3.12.0、PySide6 6.11.1。

## 已完成

| 验证 | 结果与范围 |
|---|---|
| 当前控制台源码的 SDK、扩展平台、Runner、管理、manifest、事件路由、绑定、提案与共享协议定向回归 | 103 项通过；使用原项目测试，不涉及烧录或实际设备写入 |
| 本仓库独立测试 | 11 项通过；配置 Schema / 样例、公开认证正反例、量产公钥与测试根分离、ZIP 结构和许可、提案示例驻留回归 |
| SDK wheel 构建与隔离目录安装 | 通过；安装后可导入 SDK 1.0.0 / API 1.0，包含 LICENSE 和 NOTICE，不捆绑 Qt 二进制 |
| SDK 运行文件与源项目对比 | `client.py` 和 `__init__.py` 逐字节一致；只增加发布元数据、README 和许可文件 |
| `observe_prompt.zip` 导入 / 启用 / 接收事件 / 停用 | 通过，使用真实控制台扩展管理与 Runner、合成设备状态与事件 |
| `claim_prompt.zip` 导入 / 绑定 / 接受动作 / 停用回退 | 通过，同上 |
| `propose_mapping.zip` 提案 / 保持在线 / 用户批准到草稿 / 停用 | 通过；没有直接发出配置写入，后台只读事件轮询仍可运行 |

三个 ZIP 的联合验证使用当前控制台源码和公开协议目录，不是模拟 SDK 方法；设备状态和触发事件由测试提供，因此不代表真实硬件、USB / BLE 或安装包验收。

## 本次示例修正

原始提案示例提交后立即退出，控制台会因为扩展不再就绪而阻止用户批准。公开版增加等待连接结束的循环，让用户有时间审阅；停用示例时由控制台结束进程。已验证用户批准后只进入本地草稿，设备写入仍需原有确认流程。

此修正位于公开示例，未改动产品固件或控制台实现。公开协议测试向量删除测试私钥字段，保留公钥验证所需数据。两个官方生产根公钥单独提供，不包含任何生产私钥。

## 用户可以复跑的部分

在仓库或完整开发包根目录，使用已安装 SDK 和 `requirements-dev.txt` 的虚拟环境运行：

```bash
python -m pytest -q
python -m pip wheel --no-deps --wheel-dir dist ./sdk/python
python tools/build_release.py --output dist
```

独立测试和打包不依赖私有项目路径或设备。控制台联合测试需要内部宿主源码，因此不作为本仓库可独立运行的测试发布。

## 未完成的范围

- 新控制台安装包、干净 Mac 安装以及官方实体设备触发全流程；
- Windows、Linux、Python 3.13 实测；
- BLE 扩展实体触发；
- 本次开发包对全部既有固件版本的兼容确认。

这些限制均保留在 [兼容说明](versions.md)，不以已通过的源码测试替代。当前发布提供 SDK、示例与协议，不提供新的控制台安装包或固件镜像。
